{% extends 'base.html.twig' %}

{% block title %}Modifier l'épisode{% endblock %}

{% block body %}
{% if is_granted('ROLE_ADMIN') %}
    <div class="container">
        <div class="col align-self-center">
            <div class="text-white">
                <h1 class="text-center">Modifier l'épisode</h1>

                {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('episode_show', {'slug': episode.slug}) }}">Retour à la fiche de l'épisode</a>

                {{ include('episode/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endif %}
{% endblock %}
