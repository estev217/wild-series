{% extends 'base.html.twig' %}

{% block title %}Ajouter une nouvelle série{% endblock %}

{% block body %}
{% if is_granted('ROLE_ADMIN') %}
    <div class="container">
        <div class="col align-self-center">
            <div class="text-white">
                <h1 class="text-center">Ajouter une nouvelle série</h1>

                {{ include('program/_form.html.twig') }}

                <a href="{{ path('program_index') }}">Retour à la liste des séries</a>
            </div>
        </div>
    </div>
{% endif %}
{% endblock %}
