{% extends 'base.html.twig' %}

{% block title %}Modifier la série{% endblock %}

{% block body %}
{% if is_granted('ROLE_ADMIN') %}
    <div class="container">
        <div class="col align-self-center">
            <div class="text-white">
                <h1 class="text-center">Modifier la série</h1>

                {{ include('program/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('program_show', {'slug': program.slug}) }}">Retour à la fiche de la série</a>

                {{ include('program/_delete_form.html.twig') }}

            </div>
        </div>
    </div>
{% endif %}
{% endblock %}
