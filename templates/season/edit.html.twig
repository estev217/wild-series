{% extends 'base.html.twig' %}

{% block title %}Modifier la saison{% endblock %}

{% block body %}
{% if is_granted('ROLE_ADMIN') %}
    <div class="container">
        <div class="col align-self-center">
            <div class="text-white">
                <h1 class="text-center">Modifier la saison</h1>

                {{ include('season/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('season_show', {'id': season.id}) }}">Retour à la saison</a>

                {{ include('season/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endif %}
{% endblock %}
